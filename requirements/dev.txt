-r base.txt

# ── Groq AI (Development inference) ─────────────────────
# Only needed when AI_PROVIDER=groq

groq==0.11.0

# ── Local Embeddings (Development) ──────────────────────
# Only needed when AI_PROVIDER=groq (uses sentence-transformers locally)

sentence-transformers==3.0.1
transformers==4.45.2
torch==2.9.1
scikit-learn==1.7.2

# ── Testing ──────────────────────────────────────────────

pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
pytest-mock==3.14.1
factory-boy==3.3.0
faker==37.3.0
respx==0.22.0

# ── Code Quality ─────────────────────────────────────────

ruff==0.11.13
mypy==1.16.0
pre-commit==4.2.0

# ── Dev Utilities ────────────────────────────────────────

ipython==9.3.0
flower==2.0.1
